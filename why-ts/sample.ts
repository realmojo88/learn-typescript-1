function sum(a: number, b: number): number {
  return a + b;
}

console.log(sum(10, 20));
var result = sum(10, 20);

/**
 *
 * @param {number} a
 * @param {number} b
 */
const add = (a, b) => {
  return a + b;
};
